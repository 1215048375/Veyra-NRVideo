{
  "schema": 1,
  "description": "DAV2 depth dependency manifest — Launch V1 optional depth provider. Each entry must be verified against the official source before use. Unknown or non-commercial-use-only models must not be used.",
  "status": "pending-verification",
  "entries": [
    {
      "name": "depth-anything-v2-small-fp16.onnx",
      "role": "DAV2 Small FP16 depth estimation model",
      "officialSource": "https://github.com/DepthAnything/Depth-Anything-V2",
      "expectedVersion": "v2.0",
      "license": "Apache-2.0 (model weights)",
      "licenseUrl": "https://github.com/DepthAnything/Depth-Anything-V2/blob/main/LICENSE",
      "commercialUse": true,
      "sha256": null,
      "localPath": "third_party_local/depth/depth_anything_v2_small_fp16.onnx",
      "inputShape": [1, 3, 518, 518],
      "inputFormat": "NCHW, RGB normalized [0,1]",
      "outputShape": [1, 1, 518, 518],
      "outputFormat": "relative depth (inverse depth), FP16, [0,1]",
      "notes": "FP16 ONNX export for DirectML inference. Must verify hash after download."
    },
    {
      "name": "onnxruntime-directml",
      "role": "ONNX Runtime with DirectML execution provider",
      "officialSource": "https://github.com/microsoft/onnxruntime",
      "expectedVersion": "1.20.0",
      "license": "MIT",
      "licenseUrl": "https://github.com/microsoft/onnxruntime/blob/main/LICENSE",
      "commercialUse": true,
      "sha256": null,
      "localPath": "third_party_local/depth/onnxruntime/",
      "notes": "NuGet package Microsoft.ML.OnnxRuntime.DirectML. Must pin version and verify."
    }
  ],
  "autoFallback": {
    "description": "When depth model is unavailable or Auto mode determines depth is harmful, the system explicitly falls back to zero depth with honest logging.",
    "mode": "explicit-zero-depth",
    "logMessage": "Depth provider unavailable/degraded; falling back to zero depth (NOT DAV2)"
  },
  "verification": {
    "required": ["sha256 match", "version match", "license verified", "input/output shape verified"],
    "status": "NOT VERIFIED — model files not yet downloaded"
  }
}
