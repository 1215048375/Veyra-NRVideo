# Motion corpus and scene-boundary repair

Phase 7 optimization remains in progress. Cycles 71–73 are a scoped correction, not full Q6/Q7 completion. Export integrity checks are unchanged.

## Findings and change

The original 600-frame cut/flash/duplicate clip contains scene transitions at zero-based frames 150, 300 and 450, plus a white flash at 301. The previous two reported cuts were actually flash appearance/return at 301/302. All three scene transitions were missed: their normalized SAD was .1041/.2118/.2142, below the mandatory .3 threshold, despite histogram distance .9449/1/1.

The shared scene analyzer now additionally resets on SAD > .08 AND histogram L1/2 distance > .9. The existing .3/.5 path remains. Logs identify the frame, PTS, scores and candidate reason. This is a conservative temporal-discontinuity heuristic; a counter includes exposure transitions and is not a count of semantically proven cuts. Equal-histogram scene changes can still be missed, and large exposure changes can reset. No future frame, model, new pixel readback or export verification was added.

Cycle 71 independently extends the known-vector GPU fixture to vertical motion, top/bottom boundaries and cost 16/31/32/255. Both axes pass: legacy 256 rejected/768 retained, validation 512 rejected/344 retained/168 attenuated, debug errors 0. Product shader unchanged in this cycle. Scene unit tests now contain 14 checks including moderate-luma replacement and negative controls.

## Actual execution

All paths below are under `logs/optimization-goal-20260908/`. Each GPU child has a 275-second watchdog; observed corpus children took 8.3–9.6 seconds. Build: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/build.ps1 -Root "$PWD" -Preset x64-release`, log `build-cycle73.log`, exit 0.

- Before scene fix: `motion-corpus-17987af4f4a540daba704b7dd0d4db29/`, five 600-frame 1080p clips, same EXE validated vs `--legacy-motion`. Each child exit 0, NR 600, debug errors 0. These are NR/NVOF runs, not FG runs. The initial runner `motion-corpus-cb15c7b16c3f45f1a518011e497ae722` failed because its process ExitCode was unavailable; a retained Handle fixed the runner, and the complete matrix was rerun.
- Diagnostic statistics are 64×36 samples every 60 frames, 23,040 samples per clip, **not final-frame statistics**. The original summary's `Final` property names were wrong; raw records are retained. Translation nonzero samples stay 23,040; occlusion falls 20,216→19,554, particles 16,327→16,254, UI 12,053→12,040. This proves changed gating, not improved perceived quality. Mixed GPU command-list timing is not isolated shader time.
- Source Y-plane diagnostic `scene-source-*.json`, generated by `inspect-scene.py`, reproduces exact product sampling, identifies the missed cuts, and predicts zero new candidates in the other four clips.
- After scene fix: `motion-corpus-4f1a465d927b4194b7386a74e56f900c/`, five validated 600-frame runs, exit 0/debug errors 0. Four clips: 0 boundaries, NR 600/NVOF 599. Cut/flash: exactly 150/300/301/302/450, NR 600/NVOF 594. Counts are five reset candidates: three authored cuts and two flash boundaries. All frames processed.
- Actual app D3D12 NVENC 2X export: `fg-scene-b1e15c6c88b64b00aee2b6af5f8c4990/`, command recorded in `run-fg-scene.ps1`: `veyra.exe <cut clip> --export-out <unique output.mp4> --max-frames 600 --fg --fg-multiplier 2`. Exit 0; source 600, generated 594, holds 6 (five boundaries plus tail), output 1200. This is native 1080p, not native 4K evidence. Source has no audio.
- Independent FFmpeg decode of that export: 1200 frames, 1920×1080 H.264 at 120 fps. Five boundary holds at output indices 299/599/601/603/899 differ from previous real frames by mean Y error ≤ .049/255, versus current frames by 25.3–106.7/255 (`decode-check.json`). Lossy encode explains nonzero hold differences. No cross-boundary generated image was substituted at these positions. This development decode did not change product export checks.

## Remaining scope

Synthetic motion gating and boundary correctness do not prove natural temporal quality, FG interpolation quality on occlusions, or SR reconstruction benefit. Bidirectional A/B flow cost/benefit, RTX Video SR/depth candidates and natural comparisons remain pending. Physical capture/audio/latency, long stability and redistribution remain separate. Gate and independent review are recorded in WORKLOG/EVIDENCE after execution.
