# 当前交接：后端切换、真实产出与音画同步

用户已开启目标模式，当前仍在修复。当前入口：[继续修复实施记录](CONTINUATION_REPAIR_IMPLEMENTATION_2026-09-10.md) 的2026-09-11部分；任务合同见 [继续修复方案](CONTINUATION_REPAIR_PLAN_2026-09-10.md)。不启用旧Loop，不push/发布。

## 2026-09-11 当前可续接状态

最新：`78675f2`之后可控音频重锚已加入真实5ms PCM淡出和端点消耗后Reset，最长80ms仅边界等待，正常pump不加等待。55项音频、7项真实player、合成采集通过；最终delivery `6304c4518f864f34a4bb91392cd16ab1`23项PASS，应用SHA `A86DB75E321677C2B9EDC0380D3BC4DCC13FAD3DE5BA05D59DA5C1C30C6BB97A`。物理拔插已经发生的断音不能事后淡出，未外部听觉/实卡验收。完整命令/证据在[实施记录](CONTINUATION_REPAIR_IMPLEMENTATION_2026-09-10.md)。

下一条唯一任务转reset/重建计时：`FrameFlowMetrics`仍缺排空/销毁/创建/预热到首有效输出；`DiagnosticHistory`64条去重错误不是逐帧8192性能轨迹。按原方案补真实边界与回滚/取消证据，不把process总耗时冒充reset。AMD完整网络/provider、真实系统150%/200%DPI和实卡未完成，目标active、不发布。下方“下一步淡出”是历史快照。

**当前优先读本段及实施记录最上方，后面的“下一条文件音频恢复”已完成软件闭环。** 基线`bba7283`后实现文件WASAPI owner线程重建、500ms重试、共享clock生命周期保护、断开时共同媒体时钟保持、最后有效PTS重锚、断开期间seek和暂停恢复。最终47项音频/7项真实player通过，原生4K过载/合成采集回归通过；delivery `5c80d299087b4f2482706bf2a24df9c9`23项42.420秒PASS，当前EXE `E2EA9CB93CB290E6B13683647835B3E7F54EA21810FC358D69DB803AA9168866`。具体命令、失败与身份见[实施记录](CONTINUATION_REPAIR_IMPLEMENTATION_2026-09-10.md)。未改系统默认设备、未实卡/AMD、未发布。

下一条唯一任务：可控音频重锚的短淡出与边界（现有淡入已保留，突然拔掉硬件本身无法淡出），随后逐项审计原方案计时/reset覆盖。当前自动重连针对已打开端点失效，默认设备切换但旧端点仍可用时未主动迁移。AMD provider/权重/预览环境仍缺，150%/200%只是应用注入、真实系统高DPI与实卡未测。全目标保持active。

最新续接：UI批次已提交`ee46c5f`。AMD隔离probe已抓到准确loader原因：`Preview releases of D3D12Core require Developer Mode.`，测试`continuation-amd-debug-loader`exit1/0.088秒；这是运行时加载限制，尚未查询显卡能力。本轮未开Developer Mode，网络权重/provider/目标硬件依然欠缺。下一条独立任务改为文件音频设备恢复：`src/sink/WasapiAudioSink.cpp`的`runOnAudioThread`在pump失败直接退出；先处理renderer由引擎读clock与音频线程使用的所有权关系，再恢复，不能盲目shutdown造成竞态。AMD留为未完成，下面“下一步AMD”是本次诊断前状态；目标active。

**以本段为当前状态，下方“下一条全DPI”等是历史快照。** 在`db98af7`之后修复最小窗口状态区没有明细、设置滚轮跳过选择器、DPI字体变化与窄标题范围。新增真实进程UI脚本`python scripts/acceptance/ui-layout-dpi.py`，最终`ui-layout-1789065295999940200`PASS，96/144/192应用缩放、最小窗、展开/滚动/弹窗/连续resize/最大化/全屏/空视频黑色；真实Windows只测DPI96，不能宣称跨显示器150%/200%完成。绘制21项/popup14种/布局合同及真实FRUC->XeSS->DLSS->关闭通过。当前EXE SHA `27910DEDB2855334C45A0B92F4694542AB8631FB9AA17FED819E65585CC35C55`，构建`ui-dpi-build2.log`exit0；本轮未重跑联合delivery、NR或实卡。具体失败与证据见[实施记录最上方](CONTINUATION_REPAIR_IMPLEMENTATION_2026-09-10.md)。

下一条唯一任务是AMD NR预览运行时/网络依赖/provider。当前没有AMD NR实现，缺权重、可用SM6.10/linalg设备创建与目标RDNA4实测；不能把AMD光流或选择项当完成。音频文件端点自动恢复、突变淡出、真实系统DPI与用户实卡也仍待收尾。单GPU所有者、FRUC像素与音频漂移的前序证据继续有效，但整体目标未完成。不启用旧Loop、不push/发布。

当前最新：`e25585e`之后单GPU所有者主循环已落地，`LiveGpuScheduler`取代并删除旧呈现线程，所有图/完成/Present由引擎线程推进；采集tryRead不阻塞，源Waiting仍推进帧，EOF完整交付62真实帧。状态机15/live30/EOF6/FRUC24/正常与失败XeSS UI通过，最终delivery `3462b28549ed4982a99799adc5a3071e`23项42.380秒PASS，EXE `1B0DF186574B552E349EBB9502AF89A001430B89407B3D15635862E668279CE6`。旧`e25585e`独立构建在`out/owner-baseline`（干净源码、ignored SDK/runtime junction），同源原生4K重载A/B均33fps、P95约59.66ms，没有性能提升结论。下一条唯一任务全DPI/最小窗/专业展开滚动与选择器验收；AMD NR provider/依赖和实卡仍未完成。完整命令、失败、限制见实施记录最上方，下面“下一步单GPU所有者”是历史快照。无发布，目标active。

最新：音频修复本地提交`1821120`。其后GPU阶段计时的完成记录逐帧交付、同源补帧空闲查询槽选择已修；RTX6项/CPU71/live30项通过，源30fps对应Color30样本。联合delivery `88753f608465497dba0c2660bf7755dd`23项42.146秒PASS，EXE `F6B5F0E96B81B223FE856E888221CFF51ABBE706C9898A6609A49B7EF90413A2`。下一任务完整单GPU所有者状态机，继续解决阻塞观测与末帧计时；尚非S3完成。详见实施记录最上方，下面“下一步逐帧交付”已由本段覆盖。AMD NR/全DPI/实卡仍未完成，目标active。

当前优先续接：`ed5aed3`之后漂移补偿/真实PCM时间映射的软件回归通过，关闭/零补偿53.8ms回归经采集静音填充/事件顺序修复。最终普通/端点丢失/文件与断粮时钟/70项CPU通过；同一EXE正负120秒漂移P95为4.242/4.302ms，均仅首个reset。delivery `c41ad55982e6448190d1bc4ed239a415`23项42.484秒通过，应用SHA `FDA98B226562C55B44A8139E1A4D7F3F35A7458109AC8ED4F72FD6ACE755D6BE`，本轮建立本地checkpoint。实施记录最新“采集时钟漂移与真实PCM播放位置”段包含命令、失败和EXE身份。下一条任务：修`GpuTimer::collect`多帧收回却只通过`last()`上报最后一帧的问题，保持epoch/revision隔离，再继续完整GPU所有者。目标active，AMD NR、完整DPI和实卡尚未验收；下方31.73ms漂移失败是本轮修复前基线。

最新音频续接：raw/转换中/PCM统一500ms上限，突发与正常/断流回归通过；WASAPI错误真实记录、有限频率重连及自有端点故障测试通过，设备padding单列。最终联合delivery `0325b097b61a42b0becf6dc71a900175`23项42.43秒通过，EXE `1136543C02604020F877BAA8BE0F67442671DFC7805FBB28310CD57B2D3E8FC6`。新增120秒源时钟-1000ppm测试失败P95 31.73ms，仍靠间歇reset；下一任务必须实现平滑漂移校正和重采样后正确媒体时钟，不能拿联合短测覆盖此失败。详见实施记录最新音频段，目标active，未实卡/未发布。

最新续接覆盖下方旧计数/哈希：前序已本地存档`6316376`；音频-only设置隔离、重复请求消除、排空后合并最新设置已修；生成帧A/B真实到达关系与三项独立延迟加入状态。FRUC跳过恢复新GT暴露source parity覆盖旧CUDA输入，已改按实际SDK调用轮转，2/3/4X通过并重新启用采集admission。同源原生4K NR+VSR4+FRUC4过载：450无收益Evaluate→450提交前skip，源处理12→33fps，两组均0有效生成呈现，非4X实时或内核提速。合同66/live29/FRUC24和最终delivery23通过，run `f024e12327b5429593e762943abb2eca`，EXE `D3E8736F9D0ADD088B0CEA5FE8C979B99820745467CA7743654A93687EF24823`。详细失败/命令/worker哈希见实施记录最新段。下一条任务为音频漂移与PCM总容量/端点恢复；单GPU所有者和AMD网络仍待做，未实卡/未发布。

- XeSS选择器重叠、失败事务回滚已修；真实UI后端切换回归通过。主状态区已改为真实GPU-ready产出、呈现提交、阶段均值/P95、总延迟和音频状态；长错误文字按实测宽度换行。
- 完成观察已从单纯等待呈现时间改为在deadline等待与两批反压期间非阻塞检查，共享FrameOutputs避免重复解析；仍使用现有双线程GPU互斥，不是完整单所有者调度。53项合同、RTX23项30/60+NR/FG回放通过。
- 文件PCM时间戳、首缓冲、重采样尾样本、暂停seek和GPU过载时的音频时钟保持已修；采集音频改受控PCM/WASAPI，有自动/手动/关闭与错误降级、断流恢复。合成PTS+真实WASAPI通过，不是实体采集验收，微量漂移和平滑仍待完善。
- FRUC已切换为CUDA arrays与D3D11自有纹理桥，整批map/unmap后信号交给D3D12；保留有界资源、无正常像素回读。常规reset用bSkipWarp重新种入历史，停止重启worker和CPU排空。2X/3X/4X重置后像素/前后跳PTS通过；真实controller已知平移回放19项通过。CUDA SDK互操作仍可能阻塞，不能标完全异步。
- FRUC正确性改善不等于高倍率性能修好：1080p4X候选FG区间约26ms；初版原生4K4X约70.6ms。未完成精确旧worker同源A/B，不声称性能倍数提升。其它素材若SDK报告重复就仍显示无有效补帧。
- 最新联合交付检查：`logs/delivery/ad0a4e4680d748d0b97de6fe0e665c53/result.json`，23项PASS，42.35秒；EXE SHA `24216560E25D6C84D0BF3656E3AC70BFCD8917F2BED52AE82AB9B4BF2B168778`。实际RTX NR/NVOF、原生4K正确性、播放器、图像、NVENC H264/HEVC音轨和取消通过。不是整体目标/实体采集/AMD通过。
- AMD NR仍未接入。固定上游MIT核心取得，但缺权重/目标RDNA4。官方Agility1.721.3-preview已取得并校验Microsoft签名，仅在ignored探针目录使用；设备创建返回`D3D12_ERROR_INVALID_REDIST (0x887E0003)`，未进入SM6.10/linalg能力查询。未改Windows开发者模式、未给主程序替换Agility。上游`RecordUnsubmitted`明确整网络单列表曾挂GPU，后续必须分段提交，不能直接嵌入单pass。

下一任务：完成单GPU所有者/设置合并与生成帧时间线计量，同时解决AMD预览运行时、完整网络依赖及provider。UI完整DPI、实卡与长期体验仍需覆盖。具体命令、候选失败及数据边界都在实施记录，单次测试<300秒。

## 以下为2026-09-10开工前快照

下方“未实施”、旧EXE哈希与下一任务仅代表当时记录，当前状态以上述2026-09-11条目为准。

## 当前基线

- 本地代码提交 `be00e27`，进入本轮时工作树干净。当前 EXE 路径 `out/build/x64-release/veyra.exe`；上一交付记录 SHA256 为 `429C5600E2A4ABF8D72B83B0F658B3190FE5397FE9A11256946B924CABA560F4`，本轮未重新构建或测定其身份。
- 已实现 SR 2K/4K/8K 目标、XeSS 预览2X与 AMD FidelityFX 光流；并非下方历史段落所说的全部停留在研究。AMD NR 本体仍未实现，不能将光流测试当NR支持。
- 上一轮已完成 DLSS 采集 FG admission、session/revision/epoch 帧流账本、异步取消与部分生命周期修复；完整 S3/S4、FRUC reset像素和实卡未完成。
- 上轮最终 delivery 23项通过，42.78秒，`logs/delivery/ac24ad4a2e1e4169a171fda66e4b7e84/result.json`。这是历史基线，不是新方案通过证据。
- 旧 Loop、控制哈希、STOP 和 Phase 排队已退出当前流程。直接按新方案构建和针对性回归，单次测试最多300秒。运行时身份、源码/二进制隔离、真实验证和发布规则继续生效。

## 本轮核实与待实施

| 项目 | 真实状态与下一步 |
| --- | --- |
| XeSS入口 | 选择器ID208已有三种FG后端，但y=8与标题1103的y=12重叠；先修布局并验证实际点选/applied切换 |
| AMD / NVIDIA NR | 新增独立NR provider设置与真实AMD网络接入，不能仅用光流或不可用按钮完成交付；目标AMD硬件仍需实测 |
| 实时FPS | 当前 `s.fps` 是完成处理的真实源帧速率；新增有效补帧产出与呈现提交主指标，XeSS SDK提交明确单列 |
| 实时处理状态 | 用紧凑指标区替换大NR圆环，集中总延迟、分阶段耗时、输入/处理/生成/呈现速率和音频状态 |
| 音画同步 | 采集音频目前DirectShow renderer直通；接受控PCM/WASAPI、共同PTS与有界补偿，文件保留音频主时钟 |
| FRUC / 调度 | 重置错图证据指向旧输出；诊断CPU全同步通过，生产可用GPU同步方案未通过。继续像素/资源交接定位及S3状态机 |

实施顺序与依赖：R0选项/事务 -> R1指标/状态区 -> A1 AMD NR -> R2 FRUC/调度 -> R3音画同步 -> R4集成回归。AMD的实际硬件阻塞不阻止其它模块修复，但也不能在最终报告中抹去。详见新方案的任务表与验收条件。

下一条唯一任务：复现ID208与1103的重叠，修补帧页布局，并验证XeSS实际可选择且applied后端正确。当前尚未执行此修复，不把方案完成称为软件完成。详细历史实现、命令和失败见 [调度实施记录](SCHEDULER_REPAIR_IMPLEMENTATION_2026-09-10.md) 与 [WORKLOG](WORKLOG.md)。

## 以下为早期同日交接，仅供历史追溯

下方“最新”“下一步”、Phase标签、EXE哈希及未实施描述均属于当时记录；当前状态以本页上半部分和新方案为准。它们不再作为执行队列、门禁或当前通过证明。

### 早期主题：处理帧率与采集 60→30

最新补充：[2K / 8K 超分与 AMD NR 接入方案](SR_TARGETS_AMD_NR_PLAN_2026-09-10.md)。沿用先写方案范围，尚未改产品：SR目标明确QHD2K/4K/8K；AMD确有HIP/ZLUDA/D3D12网络移植，推荐MIT D3D12核心为RDNA4实验候选，不能把AMD光流当AMD NR。当前强制NVIDIA设备、统一NGX初始化、NVIDIA专属SR/FG/编码依赖均须拆开；8K资源及大图/导出分支须真实验证。下一实施项改为S0统一SR目标规划，Phase7与现有EXE不变。

最新研究：[XeSS 补帧与 AMD 光流接入方案](XESS_AMD_OPTICAL_FLOW_PLAN_2026-09-10.md)。仅方案，未实施：公开 XeSS 在非 Intel GPU 当前仅2X，需代理交换链与XeLL；AMD OF先单独与现有NVOF比较，保留原有后端及导出。不要把这份研究当新增效果已运行或性能通过。

最新增补：原生 UI 闪白入口已修复并完成限定软件回归，见 `docs/UI_PAINT_REPAIR_2026-09-10.md`。本机 EXE 已更新为该报告的 SHA；下方 FPS 交付 SHA 和测试仍是上一存档的历史证据。Phase 7 未晋级，用户实际闪烁场景待复核。

2026-09-10，Phase 7 `in_progress`。本地开发存档，不代表整体性能问题解决；不 push、不更新 Release、不打包运行时。

## 最新决定与实现

用户说明上一轮帧率变化发生在自己调整画质期间，不能将自然日志当作固定配置的补帧故障证明，撤回此前确定归因。本轮完成实时处理FPS、PS5稳定30fps经60fps采集的处理选项，并同步此前成果。

- 底栏右侧 `处理 x.x fps` 在日常、专业及全屏控制条可见，全屏控件自动隐藏时随之隐藏。诊断使用同一指标。
- `FrameRateWindow` 统计最近一秒GPU完成的真实帧，不乘FG倍率，不使用会话平均、源到达或CPU提交次数。`EnhanceGraph::resolveGeneration` 对无FG真实帧也非阻塞检查GPU fence。暂停/停止/图片显示0，停流一秒内归零；成功切换设置重置窗口。最多8192个事件，支持1000fps计数。
- 内容节奏选择器末尾新增 `采集60→30fps处理（PS5 30帧）`，保留旧选项和值。“识别30fps”只检测节奏，原来不会降低GPU处理次数。现有预设字段保存新枚举。
- `CaptureHalfRate` 在采集read后、GPU graph前隔帧取样，60→30、59.94→29.97。选中帧保留原始PTS，duration为两个传输周期，FG以约33.3ms间隔配对；2X/4X目标约60/120fps，不保证实际达到。
- 只对标称59–61fps的采集生效；18/30/120fps不会误减半，不适用采集显示“不适用”。文件播放与导出不丢源帧，音频路径不变。
- 每个传输包仍读出，有意跳过单独记 `captureRateSkipped`。mailbox容量1、呈现两批次上限不变；真正Drop/断点仍reset全部历史，边界帧不被取样吞掉。

这是明确的隔帧取样，不是自动去重，不能恢复PS5内部时间戳。稳定A,A,B,B输入适用；游戏帧间隔波动、HUD独立60fps、重复相位变化可能漏取独特画面，应切回源时间戳比较。

## 之前的实现一起存档

1. 原生YUY2/NV12 DirectShow接收、一次GPU颜色转换、范围/矩阵/stride校正、同尺寸拷贝消减。离线颜色证据见 `BASELINE_REPAIR_IMPLEMENTATION_2026-09-09.md`；用户撤回的实卡同帧A/B没有执行。
2. 两批次异步呈现。Drop使temporal history失效，但不再每次排空完成的真实帧；跨generation生成帧跳过。显式重配/resize仍排空。
3. 采集设置使用新到达帧和真实arrival，暂停可重算缓存；复用DeadlineWait。实时档光流内部尺寸限制至NR内部尺寸以内，motion再换算至NR/FG尺寸，原生/导出保留原尺寸策略。顺序仍SR→NR→FG。
4. 对比关闭不保存额外source/base参考；默认关闭成功逐帧日志，`VEYRA_VERBOSE_FRAME_LOGS=1`启用。按revision重置计时窗口，记录槽位等待、GPU各阶段、Present与过期计数，GUI写文件日志。
5. 本地FRUC依赖补齐及打包allowlist源代码变更一并存档，DLL/SDK不入Git；远端旧包没有更新。

## 实际验证

本机RTX5070，测试串行，单项外部timeout290秒，没有打开实体采集卡。路径相对项目根目录。

| 命令 / 场景 | 结果与证据 |
| --- | --- |
| `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/build.ps1 -Root <root> -Preset x64-release` | exit0，`logs/build-fps-capture30-20260910-final.log` |
| `veyra_live_timing_tests.exe` | 41 PASS：60/59.94、抖动、断点、停流归零、1000fps |
| `veyra_presentation_worker_tests.exe` | 11 PASS：有界、取消、失败、统计清空 |
| `veyra_repair_preset_tests.exe logs/capture30-presets-20260910.txt` | PASS：新选项往返、旧schema |
| `veyra_live_presentation_tests.exe loop/local/fixed_clips/test_av_1080p.mp4 logs/fps-capture30-20260910/live-half-final --half-rate` | 19 PASS；GPU约30fps、切回约60fps、NR/DLSS2X/4X、暂停/设置/退出。`logs/baseline-repair-20260909/capture30-live-final.log` |
| `python scripts/acceptance/ui-processing-fps.py` | 日常/专业/窄窗口：文字可见、边界内、实时数值、暂停0；`logs/baseline-repair-20260909/capture30-ui-final.log` |
| `veyra_live_presentation_tests.exe logs/video-sdk-trial-20260909/known-pan15.mp4 logs/fps-capture30-20260910/live-fruc --fruc` | 14 PASS；既有FRUC回放、设置、暂停、完成FPS，`logs/baseline-repair-20260909/capture30-fruc.log`。不覆盖已知reset-pixels失败 |
| `powershell.exe -NoProfile -ExecutionPolicy Bypass -File logs/baseline-repair-20260909/delivery-env.ps1 -Root <root>` | 原版delivery23 PASS，45.317秒。`logs/delivery/910fe667d2574babaa3b02a7aa9bb3e2/result.json` |

测试EXE均在 `out/build/x64-release/`。live日志有 `interval100ns=333334`、`gpuCompletedFps=30.00`、`rateSkipped=63`，切回 `interval100ns=166667`；这是处理/提交而非屏幕扫描。delivery包含实际NR/NVOF/GBV、native4K、播放、图像、NVENC音轨/CFR/取消。包装仅导入系统Utility模块，gate与导出检查没有改。

本次两项初始失败保留：独立单测Windows min宏编译失败，改括号调用后通过；UI脚本误认辅助根窗口，改为从标签取根窗口并统一DPI坐标后通过。

EXE SHA256：`A0DB6D6109EDE751A91762664A1589A90134A673BEAE2F5722313A53088BE646`。NR仍为AGENTS指定SHA，签名Valid。

## 边界与下一步

原生4K NR成本、临界负载FG deadline/帧节奏、FRUC重置后像素与重建等待、自然运动质量、PS5实卡体验和长期稳定仍未验收。软件短测不抵消 `NATIVE_NR_PERFORMANCE_2026-09-09.md` 的FRUC失败。既有控制hash漂移保留，没有改控制面自我放行；本轮无新的独立Reviewer结论。

下一条任务：用户在固定30fps PS5场景、采集60fps下选择新增模式，观察输入约60/处理约30，对比关闭、2X、4X。性能归因必须固定尺寸、SR质量、NR内部尺寸、FG倍率和applied revision。

启动 `Veyra.cmd`，实际程序 `out/build/x64-release/veyra.exe`。本地Git仅保存源码/文档，不含SDK、runtime、模型、媒体、日志，也未更新GitHub。
