# Veyra 交接 — 2026-09-09

## 当前状态

本机实验开发版；Phase7优化仍在进行。交接存档不代表公开分发许可、长期稳定性或物理端到端延迟验收。入口 `Veyra.cmd`，Release程序 `out/build/x64-release/veyra.exe`。不要移动本机runtime路径。当前说明以本文件及USER_GUIDE为准，旧报告为历史证据。

## 本次设置变更（Cycle86）

- 增强/补帧数值、选择器、开关直接请求生效，无需“应用”。每次修改仅提交自身字段，基于最新desired配置，避免隐藏页面串改NR尺寸。
- 合法数字即时提交；不完整/越界数字不进入引擎，失去焦点恢复有效值。增强关闭期间保存待启用配置；总开关事务忙时明确拒绝，稍后重试。
- 风格0/1/2按钮；自动遮罩、UI修正开/关按钮。保留实验/未证实标签，不宣称这些参数都有效。连续值保留数值框和拖动。
- 参数面板滚轮转为滚页，不改变滑条/收起的选择器。全局主题滑条禁滚轮调参，包括进度和音量；视频预览滚轮缩放保持。
- 常驻一个“还原默认”，恢复内建增强与补帧参数；预设管理/导出操作按钮仍保留。
- 用户明确删除loop/GOAL_PROMPT.md，且授权删除对应前置检查；仅移除该文件要求和保护条目，其他10个控制文件hash及二进制验证保留。manifest pin相应同步。不能恢复该文件或将本次授权扩大到任意门禁修改。

构建命令：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/build.ps1 -Root <project> -Preset x64-release`。
回归命令：`python scripts/acceptance/ui-settings-page-scope.py`（原Cycle85手动提交语义已被实时设置取代）。
Maker构建通过，实际RTX UI回归通过：`logs/video-sdk-trial-20260909/build-cycle86.log`、`ui-live-cycle86.log`；底层参数日志 `logs/settings-page-scope-1686641c298044738edc91e075662790.log`。初次脚本控件计数错误15/实际14已纠正，不是产品失败。独立审查和最终gate结果待追加。

## 既有改动一起存档

- Cycle78：SR运动响应诊断，未擅自更改默认算法。见SR_MOTION_RESPONSE_2026-09-08.md。
- Cycle79：窗口拖拽/缩放栈溢出修复，AppShell大局部数组改为按需堆缓冲；旧独立短测通过。
- Cycle80：取消采集格式30/60fps白名单；USB3Video公开4K18 YUY2/MJPEG，固件没有1440p。见CAPTURE_CARD_AUDIT_2026-09-08.md。
- Cycle81–83：RTX Video SR官方本地SDK试验与可选共享后端接入。开关决定SR启停，下拉选择DLSS或视频SR档位；SR→NR→DLSSG顺序不变。FRUC有独立D3D11试验，未接入产品。见RTX_VIDEO_FRUC_TRIAL_2026-09-09.md及RTX_VIDEO_SR_INTEGRATION_2026-09-09.md。
- Cycle84：真实1080p60卡对照，原生4K NR约23ms、实时NR约6ms。视频SR最高约6.5ms、低约1.5ms；实时NR+低SR接近60源fps。不能把这些阶段耗时当物理延迟。测试恢复时遗漏FG2、留下关闭，已向用户纠正。见PHYSICAL_CAPTURE_DIAGNOSIS_2026-09-09.md。
- Cycle85：用户FG开关导致NR22→6ms，日志证明NR4K→1080；旧设置整页提交存在串改缺陷。已修复并独立通过；Cycle86进一步改为单字段即时提交。见SETTINGS_PAGE_SCOPE_REPAIR_2026-09-09.md。

## 下一位接手先做什么

1. 查看git存档、loop/STATE.json与本报告末尾最终验收结果，保留用户本地配置和未提交改动。每次测试上限300秒，实卡由用户验收，不自动占用。
2. 优先修正性能指标窗口：EngineController TimingWindow仍保留1200历史采样，改配置后统计会混入旧值。UI必须区分NR单阶段、其他GPU阶段、处理吞吐与未测物理延迟；不能相加后伪称实测端到端。
3. 既有间歇player-sync时序失败仍需隔离复现；一次phase7通过不能抹掉历史失败。避免用户实卡运行与GPU门禁并发。
4. 自然运动画质、FG伪影/节奏、实卡音频/主观延迟及长期稳定仍由真实素材验证。FRUC若后续接入须解决D3D11/D3D12互操作、明确一帧lookahead成本，不冒充SDK已自带D3D12补帧。

正常视频目前软件解码；RTX5070负责SR/NR/光流/FG/呈现。Depth provider未接入；禁止宣称等同游戏原生运动/深度。导出完整性检查保持现状，不加重。

## 许可和存档边界

仅本地Git源码/文档存档，不push、不发布、不打包。runtime_local、third_party_local、SDK压缩包、个人媒体和测试输出仍忽略；两个固定专有二进制不得提交。Git记录不含完整可运行环境，后续仍需原本机依赖。


## Cycle86 最终独立验收与交接

只读review_video_sr限定范围PASS，无新增P0/P1/P2。preflight69 PASS，phase7 73 PASS，实际命令 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/loop-gate.ps1 -Gate preflight` / `-Gate phase7`；delivery `logs/delivery/9f7468b334bc457db4b8bf125d4f5ddb/result.json`。减少的2项仅为用户明确删除的GOAL_PROMPT文件存在/hash检查，剩余10个控制文件身份保持。

实际RTX独立UI回归通过（`python scripts/acceptance/ui-settings-page-scope.py`）；额外回归确认master-off保存.55/style1/FG2、重新开启真实应用；焦点内无效文本保留，失焦恢复；注入style2失败后回滚style1且按钮状态同步；滚轮确实滚页但不调参；还原默认生效。证据 `logs/video-sdk-trial-20260909/review86-ui.stdout.log`、`review86-extra-final.stdout.log`、`logs/settings-page-scope-79d029d4883e42fea08d53eecfcc65c6.log`。初次补测1秒等待不足，改为最长5秒观察真实回滚后通过，未用固定短等待误报产品失败。

EXE SHA256: `3BD470D51AD43F7C461F23AB6F9F9B7234DC6F33D84C9DDEB35A463663264E2C`。构建日志 `logs/video-sdk-trial-20260909/build-cycle86.log`；独立review前后跟踪指纹 `9d45fe7ff6b19a67b3a42db2215928b1aecf5b4f`不变，之后仅更新交接记录。单次测试均不足300秒。Cycle86修复可交本机实测；整体Phase7优化仍in_progress，既有统计窗口/间歇时序/实卡体验/长期稳定风险未宣称消失。下一任务见HANDOFF优先处理配置相关统计窗口。

本地Git checkpoint包括Cycles78–86已核对源码、使用指南、交接报告与授权的GOAL_PROMPT删除；不包含runtime/SDK/个人媒体，不push。实际提交ID以本次 `git log -1` 为准，避免在commit内容中制造自指hash。
