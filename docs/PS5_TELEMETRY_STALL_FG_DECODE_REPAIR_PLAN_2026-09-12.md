# PS5状态面板、停帧、补帧降级与解码切换修复方案

> 最新状态：本轮代码、构建和自动回归已完成；原始 PS5 偶发停帧未重现，新增恢复及硬解等待用户真实连接验收。见 [验收入口](PS5_REPAIR_ACCEPTANCE_2026-09-12.md) 和 [执行证据](PS5_TELEMETRY_SCHEDULER_EXECUTION_2026-09-12.md)。下文“尚未施工”是最初排查时间点，不代表当前状态。

日期：2026-09-12；基线30694da；状态：排查与方案，尚未施工。本轮只读代码/日志、保存忽略目录证据并更新文档，未改产品代码、未运行新GPU负载、未发布。用户补充：“ui可以，只是画面卡了”。声音/手柄是否继续未确认。

## 一、已确认的事实

### 面板统计

位置：apps/veyra/ui/LiveStatusPanel.h、include/veyra/engine/FrameFlowWindow.h。

1. “软件总延迟”是softwareLatencyMs，实际为一秒滚动样本平均值；“总延迟P95”为同窗95分位，250ms更新。因此截图32.0ms和43.6ms不同是正常统计结果，但标签没有说清楚，不应让用户比较两个不明口径的“总延迟”。
2. PS5的计时起点是Chiaki完整视频样本回调arrival100ns，终点为对应原帧Present返回。包括回调后排队、解码、增强、呈现；不含PS5渲染编码、此前网络/组帧和屏幕扫描。文件提前处理的驻留也不能直接解释成音画落后。
3. “处理产出”是GPU完成原帧+有效生成帧；后者可能随后过期。截图76=59原帧+17有效生成，与59次呈现不矛盾，但大字76容易被误认为实际送显帧率。
4. PS5底栏已接remoteReceivedFps/remoteDecodedFps，这个面板却仍按capture标志取captureFps，所以输入显示0：确定的面板接线错误。
5. “取帧/解码”测Engine::read消费耗时，而PS5实际FFmpeg解码在独立owner，当前数字大多只是取邮箱，不能代表解码耗时。
6. CPU提交、GPU执行、GPU就绪等待有重叠；各阶段P95并非同一张帧。禁止把它们相加当总延迟。

### 补帧：旧方案原则正确，当前实现需要继续修

已核对docs/REALTIME_AV_SCHEDULING_REPAIR_PLAN_2026-09-11.md（9885759、5bc91cd实施记录）、SOFTWARE_AV_SYNC_REPAIR及WORKLOG历史。

旧方案要求连续音频、正常一倍速、优先省可选FG；明确不要按“FPS低于目标就切模式”，恢复要计入预热，不能频繁开关反而更慢。它没有要求“不到60就永久锁30”。保留音频连续性修复，不恢复旧holdForVideo停音。

源码核查：

- LiveFgAdmission按生成PTS截止时间、完成P95与10ms宽限进行整对准入，不是简单比较FPS。
- EnhanceGraph每次跳FG置fgHistorySkipped_，下次准入带reset，out.hasGenerated=false。若跳/允交替，会花Evaluate成本反复预热而没有有效生成帧，这是重要的反馈环风险。
- 生成完成后还受过期判定，必须联合检查准入、预热、A/B呈现时间轴；不能直接删过期判定来提高数字，那会显示旧帧并增加延迟。
- fileCompletion会混入带FG、不带FG、预热等不同成本。实时liveCompletion有500ms老化，仍需验证恢复能否持续成功；不能以一个后端/来源的结论替代全部。
- fgBudgetLimited来自累计fgSkippedBeforeEval>0：曾经跳过一次即可一直显示“预算不足”，不是当前窗口状态。这是确定的状态口径错误。
- XeSS独立使用XessGenerationGate：连续12次lateness>1.5源间隔抑制，连续60次lateness<0.5间隔恢复；要另测迟滞和恢复，不套DLSS Evaluate逻辑。

日志证据保存于logs/ps5-telemetry-audit-20260912/veyra-app.log（Git忽略），SHA256：2E498C4C1681906972517A99030E972DD2BAA067A42F65914C39C2A704A0A26A。

session2/revision5/epoch16结束累计：537原帧呈现；537FG候选，347次提交前跳过，190次Evaluate，其中95次预热、95张有效生成；有效生成只有19张呈现，76张过期。最后若干窗口原帧约60fps、呈现也约60fps。证明大量生成计算没有转化为呈现，不能只归因于5070性能不足。但此段是60fps输入，用户描述的30→60还需同设置复现，不能据此宣称永久锁30的完整根因已确认。

### 停帧：UI活着，不能据此断定GPU死锁

日志是UTC，18:10对应本地次日02:10：

- 02:10:23发生revision5设置重建，总1691.889ms，创建1432.729ms、销毁239.140ms。这会产生一次明显停顿，但之后24–31秒仍持续输出，不能用它解释后续所有冻结。
- 最后周期输出02:10:31.804，02:10:33.422有两条audio-reset，02:10:51.626才有关闭统计。后续GPU排空和资源释放正常，未见该段device-lost或FFmpeg错误。
- 关闭计数比最后周期统计多53张源帧；日志停止时不等于画面立刻全停。缺独立接收/解码/呈现心跳及停止原因，不能区分断流、等IDR、线程阻塞或用户停止。
- RemotePlaySessionSource无解码输出的失败超时目前30秒，期间可能保持最后画面；Engine停止推进时遥测也会过期。用户确认UI可操作，范围应定位到视频链路，不说整程序崩溃。

## 二、面板布局与数据契约

### 补充证据：用户复现4K30原生NR＋DLSS2X

用户在本方案之后复现并提供参数，已读取本机新日志。证据保存logs/ps5-telemetry-audit-20260912/file4k30-native-nr-2x.log，SHA256 EE5C5302EFCA80013C85072002670A2E4DA49272D7925FF9C736F37F85611B3B。

- H264、3840×2160、avgFps=30、软件解码threads=4。最初实时NR内部1080p，之后本地02:24:38.758改为NR/flow/FG均3840×2160；02:24:40.134应用revision4，DLSS multiplier2。
- revision4结束统计：sourceAccepted689、realSubmitted690、realPresented689，FG候选690、提交前拒绝682、Evaluate8、预热1、有效7、生成呈现0、有效生成过期7。无预览原帧跳过。统计在本次设置窗口内，初始回放/取消导致提交和呈现差1，不能把690全部当独立新媒体帧。
- 稳态每秒约30张源帧和30次呈现，playbackSpeed约1.00。表现确为只呈现原帧，不是只有UI错误，也不是通过媒体慢播造成30。
- GPU NR P95约21.6–22.2ms，光流区间约6.3–6.6ms，GPU就绪等待P95约29–30ms，图提交P95约2ms；FG阶段保留约2.58ms样本，但此窗口仅8次执行，不能当之后持续FG实测成本，更不能说全部阶段P95可相加。消费取帧约0.2ms不代表CPU完整解码成本。
- 本例只有1次FG预热，主要问题是长期准入拒绝，不是上一PS5会话的95次反复预热。必须保留两种失败用例。

结合代码的强疑点：文件路径先处理一批，再等该批原帧PTS呈现完才继续下一批。30fps下A到B间隔33.33ms，A/B中间生成帧应在A+16.67ms呈现；若显示A后才开始处理B，现有约30ms就绪成本已经超过中点加10ms宽限。最初无预测样本时8次被允许，其中7张有效结果全部过期；有8个完成样本后准入转为长期拒绝，与日志吻合。仍需逐对clock/deadline trace核实起始锚点与排队贡献，不假称已完成独立复现实验。

P1必须新增“文件预处理与呈现解耦”的闭环：按同一媒体/音频时钟提前处理未来文件帧，建立严格有界GPU结果队列，不能只预解码而仍等A显示后才增强B。按现有双槽/lease/fence审计安全复用，不复制播放循环或无限缓存。不通过倒退时间戳、长期放慢声音、无限增加音频延迟、显示过期插帧解决。打开/seek可有明确有限预热，稳态声音连续。该修复优先于单纯调准入阈值和硬解接入。现有成本不证明必达60，也不证明硬件最多30；需修调度后测实际持续吞吐。

旧方案§3.1明确要求有界提前处理窗口，此处实现并未充分解决“原帧截止前已就绪，但中间插帧早已过期”的情况。本次将其记录为旧调度修复缺口；仍保留连续音频等已正确的规则。

四组信息：概览、链路、PS5来源信息、折叠高级诊断。概览固定可见，窄面板纵向排版，不挤长术语。

### 概览

| 字段 | 含义 |
| --- | --- |
| 呈现提交 | 59fps；明确不是屏幕扫描实测。XeSS仅用SDK可确认的提交数 |
| 请求目标 | 60fps=源30×2；PS5传输60不等于游戏内容60 |
| 本机画面延迟 | 平均32.0ms / P95 43.6ms，同一行、同窗同一原帧群体 |
| 当前状态 | 正常 / 部分补帧 / 恢复预热 / 原帧欠速 / 等待视频 / 重建中 |

PS5副注“完整视频帧收到→Present返回，不含主机和屏幕”；文件用“预览驻留”。一秒窗250ms更新，无新样本显示“未测/数据过期”，不保持旧数冒充实时。P95用提示解释为95%的有效样本不超过该值。

### 链路耗时

按真实先后显示：接收后排队 → 解码 → 解码后待处理 → 上传/颜色 → SR → 光流 → NR → 残差 → FG → 输出/呈现。默认平均ms，P95在详情；关闭、预热、不可测分开，不用0代替未测。

GPU执行/队列区间与CPU等待单列，注明重叠。FG显示整批成本与倍率；A/B等待用真实时间点，不拿固定1/f填表。不制作伪精确可相加的瀑布图，不把所有分段P95相加。

### PS5串流信息

- 协商格式/分辨率/帧率，请求码率与实测Mbps分列。
- 收到完整视频帧fps、解码fps、送增强fps、原帧呈现fps、生成帧呈现fps；源码中各阶段计数不能互换。
- 实际解码器/适配器/回退原因；距最近视频接收、解码、呈现多久；等待关键帧和请求次数。
- 受损/丢失源帧、FEC恢复、接收队列丢弃、解码邮箱覆盖分别列，默认窗口增量，累计折叠。
- Chiaki有session.rtt_us，但来自连接建立时Senkusha测量；失败可能赋默认1000us。必须有有效性和时间标记，默认值不能显示“实测1ms”，建立时测量不能叫实时ping。
- 实时RTT须复用核对过的协议采样；不静默加高频探测。RTT不是单向视频延迟，不能直接÷2再加本机耗时称为端到端。
- 组帧耗时需同机最早包和齐帧两个时间点，现有完整样本callback没有起点。主机渲染/编码、单程网络、显示扫描无可靠同钟测量，显示“不可直接测”，不估一个数字冒充实测。

### 高级诊断

GPU完成产出、算完未显示、FG提交前跳过、预热、原帧预览跳过、样本数、命令槽/队列高水位、重建分段、音频缓冲/补偿/软件偏差。首页不展示revision/epoch/fence。窗口状态与历史累计分开，不能因为一次丢帧永久报警。

## 三、施工顺序

### P0：独立进度与停帧恢复

网络回调、解码owner、GPU提交/完成、Present成功分别记录最后时间、序号、session/revision、错误和有效性；轻量快照不依赖GPU每帧发布。1Hz汇总与状态变化限频输出，不逐帧刷盘。

无新画面约1秒显示阶段提示，明确区分暂停/设置重建。停包、参考损坏/等IDR、解码无输出、GPU fence不前进、Present失败分别测试；复用现有受限IDR请求。错误资源通过原有统一生命周期回收，不能强杀SDK线程或在fence前释放纹理。自动重连有次数/退避上限、用户停止优先，不无限重建。

专项核查此次双audio-reset原因；不能将普通NR耗时波动当音频断流。声音继续与否目前未确认。

### P1：修FG准入—预热—呈现闭环

1. 记录每类准入/拒绝/预热/有效/过期/呈现的窗口数、截止余量、预测成本、A/B间隔和队列年龄，建立能失败的回归。
2. 拆开原帧、FG追加、恢复预热成本；按后端/尺寸/revision保留有效样本，避免混合成本与陈旧P95自抑制。
3. 显式运行/欠速/有限恢复探测状态；恢复需要连续预热加至少一个能出有效帧的机会，不能允一次预热、下次又拒绝。保留deadline和队列上限，不能无条件强制Evaluate。
4. 容量约50–55时30→60允许稳定部分FG，不因未满60整段只输出30；若余量确实不够预热，可暂时原帧，但必须解释原因并在负载降低后恢复，不永久锁档。
5. XeSS单独验收SDK启停、实际计数、迟滞恢复。不能用Present次数×倍率制造有效输出。
6. 音频保持1×连续、真实PTS与均匀预览跳帧；不恢复停音、不悄改NR/SR画质、不放宽过期窗口换好看数字。用户设置2X不被改写，实际达成量单列。

### P2：面板与PS5数据接入

FrameFlowMetrics/RemotePlay snapshot先定义单位、窗口、帧群体、有效性、时钟，再接LiveStatusPanel和TelemetryWindow，共用格式化契约。真正解码耗时在FFmpeg调用处采样；B帧/单包多帧对应正确frameID。Chiaki可变数据由owner安全导出，不由UI跨线程裸读。

### P3：软解/硬解可选

建议“自动（优先硬解）/硬件解码/软件解码”，请求与实际路径分列。自动初始化失败可一次明确软解回退；指定硬解失败应提示选择软解，不假显示硬解。切换重开解码、等关键帧并重置历史，不能逐帧切换。

复用已有FFmpegVideoDecoder::openD3D12VA和MediaFileSource硬解基础，适配PS5 Annex-B输入；不复制第二套播放循环。RemotePlaySource当前CPU格式检查、clone和输出队列不能直接容纳D3D12VA帧，必须一并适配GPU资源引用、fence和生命周期，否则只是无效开关。

使用同适配器纹理进入EnhanceGraph，避免硬解后下载CPU再上传。视频解码队列与增强队列用GPU同步，不每帧CPU等待，不阻塞音频/手柄。保留PS5显示曲线、range/matrix/transfer，软硬同码流校验颜色与PTS。

软解确实把解码放CPU，但不能替GPU算NR/SR/FG。NVDEC是独立视频引擎，仍共享显存带宽、功耗等资源；因此“增强GPU占用高必选软解”不成立。软解还有CPU消耗和上传成本。以CPU、Video Decode、3D/Compute、Copy、显存、有效呈现及本机驻留A/B决定，不能只比较一个GPU总百分比。

NVIDIA原始资料：https://docs.nvidia.com/video-technologies/video-codec-sdk/13.0/nvdec-video-decoder-api-prog-guide/index.html 。该资料证明专用引擎性质，不证明本软件必定降多少ms。本次不新增SDK/运行时分发。

## 四、验收与存档

单次测试<=300秒，常规30–60秒；P0/P1/P2/P3大节点本地Git与WORKLOG，不push/release。

| 场景 | 通过门槛 |
| --- | --- |
| 30→2X，充足→约50/55容量→恢复 | 不因未满60永久关闭；区分算完/送显；无预热振荡，恢复有进展 |
| 60→2X约110容量；30原帧只能处理15 | 音频1×不增加Stop/PCM丢弃；PTS覆盖全时间段；延迟/队列有界 |
| DLSS2X/3X/4X、XeSS2X | 各自真实调用/生成/呈现证据，不互相替代 |
| PS5/文件/采集 | 数据来自正确来源；PS5输入不再0；取邮箱不冒充解码 |
| 均值/P95确定性夹具 | 已知样本得到期望分位值；同窗同群体；停止后过期；不相加重叠阶段 |
| 停包/坏参考/IDR/改参数/暂停/resize | 阶段可定位、提示不假实时、恢复受限可取消、无悬空资源 |
| H264/H265软硬解同片段 | 实际硬件帧及fence证据，颜色/PTS/引用正确；各引擎和延迟实测比较 |
| UI及旧播放器/导出 | 首页能读懂关键指标；窄面板不截字；导出不继承预览跳帧 |

源码证据、日志证据、推测和用户验收分别报告。这次不宣称冻结根因确定、30→60已修好或硬解已接入，不将之前“测试没问题”扩展为本轮新问题已通过。

下一条唯一任务：P0补独立视频进度和FG决策证据，首先复现并修复有生成计算却几乎无生成呈现，再推进面板及解码切换。
