# Veyra 双模式界面改版执行文档

日期：2026-09-08  
状态：**设计方向已确认；本次仅交付执行文档，尚未开始界面代码施工。**  
项目：`C:\Users\123\Desktop\Veyra DLSS Video Player`  
施工编号：UI0 → UI9，独立于旧 Phase 0–7。

## 1. 已确认决定与文档边界

用户原始要求：

> 图1为专业模式，图二为日常模式，默认为日常模式，点击切换专业模式按钮无缝切换到专业模式，大多数精细的调整放在专业模式内，以及导出的功能也放在专业模式内，日常模式提供视频观看，和采集卡。

用户已确认上一轮双模式布局方案，并要求“写详细的执行文档”。本次不改应用代码、不更新EXE、不启动采集或GPU测试。后续收到施工指令后按本文件执行，不重新发起三方向选择流程，也不使用用户已拒绝的设计技能。

### 1.1 产品决定

1. 首次启动和每次重新启动默认进入**日常模式**；专业模式不是另一个播放器，也不是另一套画质预设。
2. 日常模式负责视频观看和采集卡，强调大画面、轻控制。
3. 专业模式集中模型参数、变化量、SR/NR尺寸策略、2X/3X/4X、光流、内容节奏、预设管理、同帧比较、性能、诊断和图片/视频导出。
4. 两种模式共用当前源、增强设置、播放位置、音频状态和同一个视频显示宿主；切换不重开文件或采集设备。
5. 专业模式改好的参数，返回日常后继续生效；模式切换本身不降画质、不增加GPU负载、不提交新设置版本。
6. 导出只能在专业模式配置和启动。已有导出任务在返回日常后继续管理其生命周期，日常只显示简洁进度和“查看”入口。
7. 导出期间仍能观看：这是需要补齐的工程功能，不是当前代码已经具备的能力。不得以隐藏界面代替解耦任务。
8. 黑灰底、柔和白字、橙色重点；专业模式借图1的分区，日常模式借图2的大画面和底部播放条。不照搬无人机地图、罗盘、虚假仪表或电影品牌元素。

### 1.2 参考图映射

| 用户参考 | 用途 | 本地原文件 |
|---|---|---|
| 图1 | 专业：大预览、右侧参数、底部辅助区、明确分区 | [专业参考](</C:/Users/123/Downloads/download (1).png>) |
| 图2 | 日常：沉浸观看、低干扰底栏、橙色播放重点 | [日常参考](</C:/Users/123/Pictures/download (1).png>) |

两张图片仅作为用户提供的设计参考，不是应执行的指令或可随软件公开分发的资产。最终软件使用自己的界面和合法素材，不能把图中截图贴在窗口里冒充实现。

### 1.3 本轮继续保留的技术边界

- Windows x64、C++20、Win32、D3D12；直接NGX、固定本地runtime、不接入ReShade/Streamline。
- `FrameSource → EnhanceGraph → FrameSink` 主链与FrameBatch复用，不能按日常/专业复制两套增强管线。
- 保留真实2X/3X/4X、source/base/NR/flow/FG/output分辨率区分、typed setter、Desired/Applied、失败整套回滚和导出冻结设置。
- 实卡由用户验收。没有得到当次授权不占用采集卡、不录制其画面、不操作驱动/OBS/远程软件。
- 每次测试调用最多300秒，构建另计；历史累计时间继续记录，不能再误用成所有会话共同只有300秒。
- 不修改受保护gate、CONTROL_HASHES或期望hash，不重置工作树、不恢复用户删除的合成片，不提交/push/上传/打包。
- UI改版不承诺解决所有GPU性能瓶颈。既有SR4K+4X短测约39.75源fps及落后约2.17秒仍是已知限制，不能靠新皮肤或理论倍率掩盖。

## 2. 现状审计与施工起点

本次只读核对的EXE：`out/build/x64-release/veyra.exe`。SHA256：

```text
1DFE9A6A6963A73350B7677392516DFB23E6C208FABF4514388457183DDDA266
```

它是Repair v2修复版本，不是双模式新版。现有证据见 [Repair v2交付报告](REPAIR_V2_DELIVERY_2026-09-08.md)，旧联合检查不能替代新界面验收。

| 当前文件/模块 | 已有事实 | 本次必须做的事 |
|---|---|---|
| `apps/veyra/main.cpp` | 顶部按钮自动换行；一个video子窗口；参数、统计弹独立窗口；包含采集配置、全屏、字幕、烟测入口 | 拆出窗口壳、布局、动作路由；保留行为，建立日常/专业界面 |
| `apps/veyra/SettingsWindow.cpp` | 12项输入、预设CRUD、整套应用、Desired/Applied；PresetStore是此文件中的静态对象 | 将设置视图嵌入专业侧栏；预设存储上移到共享服务，不在两种模式分别缓存一份 |
| `apps/veyra/TelemetryWindow.cpp` | GPU/CPU统计和诊断复制预览 | 拆成嵌入式性能面板和诊断视图；复用同一日志/快照 |
| `src/engine/EngineController.cpp` | 一个持久worker、一个pending任务；post会置stop；startExport也post到该worker | 模式切换禁止调用post/open/stop；后台导出需独立任务边界 |
| `include/veyra/engine/EngineController.h` | 快照未暴露完整transport/音量/静音状态；UI另存paused | 建立统一可观察状态，防止两种界面各自猜播放状态 |
| `src/engine/VideoPresenter.cpp` | 使用既有video HWND；随客户区尺寸调整swapchain，resize有100ms节流及drain | 保留video宿主，不在切换中重建graph；将显示区resize集中协调、可观测 |
| `src/sink/WasapiAudioSink.cpp` | 文件音频输出已实现；没有对UI的音量/静音接口 | 补本应用音频控制及快照，不改系统总音量 |
| `src/source/CaptureCardSource.cpp` | DirectShow采集与可选HDMI音频直接RenderStream；容量1邮箱 | 复用采集会话；暴露设备/音频状态和本会话音量能力；避免UI线程枚举阻塞 |
| `src/engine/VideoExportJob.cpp` | 本地导出函数、冻结参数、CFR验证、NVENC、partial保护 | 由独立导出作业调用同一函数/产品库，不复制增强实现 |
| `src/engine/PresetStore.cpp` | version1、白名单、原子写入、损坏保护 | 保持兼容；增加UI偏好/启动设置时不要混入预设文件的媒体/设备字段 |
| `scripts/acceptance/repair-v2.ps1` | 独立18项功能回归，单次300秒 | 保留；新增双模式UI验收入口，不弱化旧断言 |

**三个必须正视的缺口：** 现有startExport会停止播放；现有UI音量接口缺失；现有模式与媒体状态没有独立合同。不能把它们当成布局调整已经自动解决。

## 3. 功能分配与可见性

| 功能 | 日常模式 | 专业模式 |
|---|---|---|
| 打开视频、拖放、最近打开 | 常用入口 | 视频入口内 |
| 视频播放/暂停/进度/音量/字幕/全屏 | 底栏 | 预览下方，共享控制 |
| 采集卡设备、格式、HDMI音频、开始/停止 | 简洁设置弹层 + 实时底栏 | 采集面板，保留完整状态 |
| 增强总开关 | 显示 | 显示并提供各项独立控制 |
| 已存预设选择 | 轻量菜单，只选不编辑 | 选择 + 新建/复制/重命名/删除/设默认 |
| 7项NR模型参数、5项变化量 | 不出现 | 右侧“增强” |
| SR、NR尺寸策略、补帧倍率、光流、内容节奏 | 当前状态简述，不能隐藏重负载事实 | 完整可调 |
| 按住原图/同帧比较/分屏/参考来源 | 不放常驻工具栏 | 预览工具条 |
| 图片导入与增强编辑 | 不提供入口 | 左侧“图片” |
| PNG/JPEG保存、视频导出设置/启动/取消 | 无操作入口 | “导出”面板 |
| 已有导出进度 | 小型状态条，“查看”切专业导出页 | 任务状态、暂停/继续、取消、错误详情 |
| 性能 | 一行必要状态；出现负载问题时提示 | 可展开GPU/CPU数据 |
| 诊断 | 最近问题摘要，详情切专业诊断 | 详情、脱敏预览、复制、日志目录 |

补充边界：

- 专业已经打开图片后返回日常，保留该静态画面，不偷偷换文件；底栏标“图片预览”，隐藏播放/进度，提供“打开视频/采集卡”。日常没有图片导入/导出能力不等于必须关闭已打开的图。
- 日常收到图片拖放时自动进入专业图片页后执行导入；不悄悄丢弃文件。多文件拖放暂只接收第一项并提示，不新增媒体库/队列功能。
- 点击“视频/采集卡”只是选择入口或展示配置；只有明确打开文件/开始采集才切换活动源。点错标签或关闭配置层不能中断当前播放。
- 采集不是可回放视频：没有seek、虚假时长、上一帧/下一帧或录制开关。实时录制不在本次新增承诺。

## 4. 视觉规范与尺寸

### 4.1 共用视觉

以下是施工初始token，后续可微调视觉数值，但不能破坏功能层级：

| Token | 值（sRGB） | 用途 |
|---|---|---|
| windowBackground | `#101113` | 外壳 |
| panelBackground | `#1B1D20` | 专业分区 |
| raisedBackground | `#24272B` | 弹层、输入底色 |
| divider | `#34373C` | 细分隔线 |
| textPrimary | `#F2F2F3` | 主文字 |
| textSecondary | `#B8BDC5` | 次文字，不用于仅靠颜色辨认的状态 |
| accent | `#EF873E` | 播放按钮、选中、滑杆有效段 |
| success | `#7DBC99` | 已连接/已应用；同时配文字 |
| warning / error | `#E9B45B` / `#F17B7B` | 警告与错误，同时配可读原因 |

- 中文使用本机“Microsoft YaHei UI”，缺失时采用系统可用中文字体；不联网下载字体。
- 普通正文14 DIP，次级12–13 DIP，分组标题15–16 DIP，空状态标题22 DIP；数值采用等宽数字或固定宽度数值区。
- 所有尺寸以DIP计算，最后统一乘DPI/96取整；禁止对已缩放坐标再次乘DPI。
- 主控制命中区至少36×36 DIP；常用播放按钮44×44 DIP；图标16–20 DIP，提供可访问名称与tooltip。
- 面板圆角12 DIP，控件6 DIP，面板间距12 DIP，内部16 DIP；按实际内容分组，不做满屏大小一致的卡片。
- 阴影仅用于弹层；不在视频外加巨大装饰阴影或霓虹。视频保持原比例，默认contain，不裁切宽银幕内容。
- 警告不会永久占据大画面：正常状态收敛，错误时展示一行原因与动作；详细码留专业诊断。

### 4.2 日常布局

```text
┌───────────────────────────────────────────────────────────────┐
│ VEYRA          视频 / 采集卡              切换专业模式   窗口键 │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│                                                               │
│                          视频画面                             │
│                                                               │
│                                                               │
├───────────────────────────────────────────────────────────────┤
│ 00:14 ────────────────●─────────────────────────────── 01:20     │
│ 文件名       播放/暂停        增强 · 预设     音量 字幕 全屏     │
└───────────────────────────────────────────────────────────────┘
```

- 初始窗口目标1280×800 DIP，可根据当前显示器工作区缩小；不能把窗口默认开到屏幕外。
- 顶部52 DIP；底部视频transport96 DIP，其中进度命中区20 DIP、按钮行56 DIP、余量用于间距；视频区为剩余空间。
- 文件名一行省略，悬停显示名称；不把媒体绝对路径塞进主界面或截图。
- 无视频时中心只显示“打开视频”和“连接采集卡”两个主要动作、拖放提示；最近文件最多5项，路径不可用明确提示。
- 采集已运行时底部不显示进度条，改为“设备名 · 实际输入尺寸/输入帧率 · 音频状态”及“停止采集”；名义输入帧率和实测帧率区分。
- 仅鼠标停留、键盘操作和状态变化触发界面更新；空状态不持续高频重绘。

### 4.3 专业布局

```text
┌───────────────────────────────────────────────────────────────┐
│ VEYRA · 专业工作台                          返回日常模式 窗口键 │
├──────┬───────────────────────────────────────┬─────────────────┤
│ 视频 │                                       │ 增强 补帧       │
│ 采集 │              共用视频画面             │ 预设 导出       │
│ 图片 │                                       │                 │
│      ├───────────────────────────────────────┤ 当前页控件      │
│      │ 按住原图 / 切换 / 分屏 / 输入或底图    │                 │
│      │ 进度 / 播放 / 音量 / 字幕 / 全屏      │                 │
├──────┴───────────────────────────────────────┤ 应用中/已应用   │
│ 实际尺寸 · 性能摘要   展开性能 / 诊断         │                 │
└─────────────────────────────────────────────┴─────────────────┘
```

- 顶部52 DIP；左侧72 DIP；右侧参数面板初始328 DIP，可在296–420 DIP范围拖动；分区间距12 DIP。
- 右侧仅当前页内容可见，纵向滚动；底部固定区域显示“应用中 / 已应用 / 失败已回滚”，不得被长表单滚出视野。
- 左侧只有视频、采集、图片三个有名称的入口，不加无意义图标列。
- 预览工具条44 DIP，文件播放transport76 DIP；下方性能摘要36 DIP，展开后约164–220 DIP。
- 分屏拖杆只在有效视频内容矩形内移动，不拿窗口黑边当图像坐标。切换布局后按比例恢复分隔位置。
- 同帧参考复用FrameLease中的source/base references；不得新增正常路径全帧读回。

### 4.4 小窗口、DPI与多显示器

- 客户区宽度≥1180 DIP：完整三列。
- 960–1179 DIP：右栏296 DIP，左栏缩为56 DIP，次要label适当缩短但保留tooltip；性能默认收起。
- 800–959 DIP：左栏改顶部源选择；专业参数以覆盖式右抽屉显示，由“参数”按钮展开；全宽预览优先。控件仍可完整操作。
- 工作区小于800 DIP宽时仍允许窗口适配屏幕；底栏折行为两行，右抽屉不超过客户区宽度，不能为了固定最小尺寸把关闭按钮放到屏幕外。
- 100/125/150/200% DPI、1366×768与1920×1080物理分辨率均需检查；高DPI下不能仅靠把窗口做大解决裁切。
- `WM_DPICHANGED`按新DPI从原始DIP布局重新计算，不沿用逐控件累计缩放误差。
- 保存窗口的普通位置/大小，恢复时夹入当前工作区；拔除旧显示器后仍可见。全屏状态与专业模式互不覆盖，退出全屏回到进入前布局。

## 5. 状态模型：模式不是播放会话

### 5.1 唯一状态来源

建议新增UI层结构（名称可按项目约定调整，语义必须一致）：

```cpp
enum class UiMode { Daily, Professional };
enum class ProPage { Enhance, FrameGeneration, Presets, Export, Diagnostics };
enum class SourcePane { Video, Capture, Image };
struct UiSessionState {
    UiMode mode = UiMode::Daily;
    ProPage professionalPage = ProPage::Enhance;
    SourcePane sourcePane = SourcePane::Video;
    bool fullscreen = false;
    bool performanceExpanded = false;
    float inspectorWidthDip = 328;
};
```

这不是第二份PlayerSnapshot。播放状态、当前源、当前PTS、实际音量、字幕选择、Desired/Applied、错误和输出作业分别由所属服务提供快照。

禁止让日常与专业各自维护一个EngineController、PresetStore、paused、currentFile、settingsRevision或导出状态。

### 5.2 setUiMode合同

`setUiMode(target)`只允许：

1. 更新UiSessionState.mode、恢复专业页签/滚动位置。
2. 通过批量窗口布局一次提交目标矩形和可见性。
3. 更新焦点、可访问名称、mode按钮文字；使焦点不落在隐藏控件。
4. 通知Presenter最新显示区目标尺寸，等待显示线程按既有fence纪律处理。

不得调用：`EngineController::open/stop/post/startExport/requestSettings`、`source.open/close/seek`、`capture.configure/start/close`、`EnhanceGraph::initialize/shutdown/reset`或重新创建Feature。

只改变显示区大小可以resize swapchain；工作分辨率base/NR/FG不随面板尺寸改变。UI布局版本与enhancement settingsRevision必须分开。

### 5.3 “无缝”的可测试定义

- 切换中video HWND、活动source session ID、NR/SR/FG handles及同一播放任务保持不变。
- 没有因mode-change而出现SourceOpen/Close、seek、graph reset、epoch变化或settingsRevision变化。
- 原先暂停则继续暂停；原先播放则按媒体时钟推进，不回到0、不重复触发EOS重播；采集连接不重启。
- 音量、静音、字幕、选中预设、已应用参数及导出作业ID保持。
- 不展示空白/黑色过渡占位；可暂时保持最后一个有效GPU画面。显示资源resize若造成暂停要记录实际时间，不能宣称绝对零成本。
- 稳定60源fps合成文件、排除源scene cut/reset且没有导出负载时，模式切换造成的额外连续提交间隔目标不超过100ms；超过必须定位并修复，不从统计中删掉。
- UI模式命令处理耗时目标P95≤16ms，无单次>50ms；禁止在UI线程等待GPU fence、线程join、磁盘导出或设备枚举。
- 连续往返20次，句柄/GPU资源不线性增长，不重复注册事件/计时器。

可选120–180ms面板淡入，初版先实现正确的瞬时布局切换。不得逐动画帧改变video HWND大小触发连续ResizeBuffers；可先一次落到最终视频尺寸，再只对外围面板做视觉过渡。

### 5.4 比较状态的特殊处理

“输入原图/增强前底图”比较会抑制生成帧呈现，不应在日常模式隐藏地继续生效。返回日常时清除临时按住状态、将比较显示退出到增强画面，保留专业比较偏好供再次进入使用。这只是Presenter比较选择，不修改增强参数或reset历史。日志必须区分这一显示选择和增强设置。

### 5.5 启动与偏好持久化

- UiMode不持久化为启动专业；每次应用正常启动强制Daily。CLI导出worker不创建日常窗口。
- 窗口位置、专业面板宽度/上次页签、音量/静音、字幕显示偏好可保存在独立versioned UI配置中。
- 正常启动恢复最后一次**应用成功**的增强快照；无有效历史时用用户默认预设，再无则内建默认。Desired未确认的值、失败值、导出worker临时值不得写成下次启动设置。
- “设为默认”保存预设默认关系并明确说明用于恢复默认/无历史启动；它不悄悄覆盖正在播放的设置。若需载入，用户单独点“应用”。
- 不把UI偏好、最近媒体路径、设备索引混进EnhancementSettings或现有预设白名单。最近文件沿用独立存储；采集设备重连后重新枚举，不仅凭旧整数索引自动开机采集。
- 配置写入沿用临时文件、校验、原子替换；损坏保留原文件、走明确默认并展示可查看的诊断。写操作做去抖，不能拖滑杆每一像素就写磁盘。

## 6. 控件行为合同

### 6.1 传输与快捷键

| 操作 | 文件视频 | 采集卡 | 图片 |
|---|---|---|---|
| Space | 播放/暂停；不由控件文字推断状态 | 不实现冻结/回放，不触发stop | 无操作 |
| 进度拖动 | 拖动预览时间，松开提交一次seek；保持此前播放/暂停状态 | 不显示 | 不显示 |
| F11 / Alt+Enter / 双击视频 | 切全屏 | 同左 | 同左 |
| Esc | 先关闭最上层弹层；无弹层时退出全屏 | 同左 | 同左 |
| V按住 | 专业模式临时原图对比，释放/失焦立即恢复 | 专业同左 | 专业同左 |
| Ctrl+O | 打开文件，专业支持图片 | 打开后是明确源切换 | 同左 |
| Tab/Shift+Tab/方向键 | 正常焦点与控件操作 | 同左 | 同左 |

编辑框有焦点时，Space/V/方向键不得被全局拦截；Alt+Enter/F11仅按明确约定路由。隐藏控件不能仍接收键盘命令。导出快捷键即使从日常触发，也只能切到专业导出页，不直接启动导出。

### 6.2 日常增强总开关与预设

- “增强”总开关必须含义清楚：关闭时停止应用SR/NR/FG增强，恢复普通画面；不是仅隐藏效果但继续把全部GPU负载算成已关闭。
- 保存关闭前的完整有效设置作为可恢复配置；再次开启恢复它，不强制2X或重置强度。未曾开启某特性就不能凭总开关自动开启。
- 实现上区分“用户增强配置”与“本次有效启用状态”，最终仍通过唯一Desired/Applied事务入口提交；关闭不会删除专业参数值或改写预设文件。
- 关闭中的专业细调保存为待启用配置，控件明确标“增强已关闭”；用户显式开启后整套应用。不能显示参数已影响当前画面。
- 预设菜单显示存储中的真实名字；参数偏离已应用预设时显示“自定义”。比较快照忽略运行期revision，不凭名称猜是否相等。
- 日常选择重负载预设会显示“原生NR / 4X”等简述与负载警告；不能暗中改成轻量档。

### 6.3 专业参数编辑

- “增强”页：NR开关、SR底图目标、实时/原生NR策略；模型强度/局部明暗/局部结构；变化量五项；实验参数折叠。
- “补帧”页：关闭/2X/3X/4X、请求与实际能力、光流三档、内容节奏识别；解释补帧需等待下一源帧，不把采集延迟称免费未来帧。
- “预设”页：载入、新建、复制当前、重命名、删除、设默认、恢复内建默认；复用PresetStore，不重新发明存储格式。
- 数值编辑允许暂存未完整输入的“1.”等文本；按Enter或失焦时校验，非法值停留并提示，不提交NaN/半份配置。
- 普通连续参数滑杆采用短去抖（建议120ms），松开立即提交；同组更新合为一个完整快照。倍率/尺寸/光流等需要资源变更的控件只在用户完成选择后提交，不随拖动反复重建。
- Desired与Applied均可查；应用中显示请求值和当前生效值。失败回滚整个事务，并显示失败来源，不逐字段伪装部分成功。
- 从专业返回日常时，正在输入但尚未提交的文本保留为编辑草稿，不能因隐藏窗口自动提交非法数值。
- skin=-1使用明确“默认/未指定”选项，其余0–2；style为0/1/2整数；autoMask/UI为开关。肤质/UI效果未证实标签保留，不虚构官方含义或预设。

### 6.4 音量与字幕

- 新增本应用volume[0,1]、mute状态命令及快照，UI只更改当前应用音频，不调用系统总音量/设备主音量接口。
- 文件音频在现有WASAPI音频线程对float PCM应用短渐变增益，或采用经核实的本session增益；避免重新启动音频时钟和瞬时爆音。
- HDMI音频通过当前DirectShow音频渲染链获得本graph音量能力；COM操作在所属线程执行。能力不可用时显示“此音频输出不支持应用内音量”，不能画一个没有作用的滑杆。能静音也必须验证真实输出路径。
- 无音轨/未选采集音频时明确显示“无音轨/未启用监听”，避免显示虚假的有效音量状态。
- 字幕提供当前外部SRT显示开关、载入外部SRT与基础大小选择；不新增内嵌字幕解码、字幕烧录或翻译。
- 字幕、播放条和OSD只在最终显示层叠加，不能进入NR/FG输入。普通视频导出继续遵守原有不烧录字幕的边界。

## 7. 导出与观看并存：必要的工程改造

### 7.1 当前障碍与选择

当前`EngineController::startExport()`调用`post()`，会设置播放stop并把导出放到同一个worker。新UI若继续调用它，“返回日常继续看”就是假功能。

**本方案选用独立本地导出worker进程**：由当前Veyra.exe的无界面子模式调用共享`exportVideo()`和产品库，前台播放器保留原会话。不是再复制一套算法，也不同时在同一进程里盲目初始化第二套NGX全局状态。

现有`--export-out`证明无界面入口已存在，但完整冻结预设传递、进度/取消IPC、子进程生命周期仍待实现，不能直接当现成功能。

### 7.2 作业合同

建议新增`ExportJobManager`与只读`ExportJobSnapshot`：

- 一次最多1个活动导出任务；不增加无界队列。重复点击开始时展示现有任务。
- 状态：Idle → Preparing → Running ⇄ Paused → Finishing → Succeeded / Failed / Cancelled；取消请求与取消完成分开。
- 启动时冻结完整已应用设置、输入路径、输出路径、codec和导出尺寸策略；图像/视频路径不允许混用，采集源不支持“导出视频”按钮。
- 导出固定原生质量策略，不能继承日常实时NR降分辨率；冻结的settingsRevision和当前播放revision分别显示。
- 用户之后调播放参数、切模式、打开另一个视频，不影响已启动作业的源和设置。
- 工作进程使用已验证的当前EXE绝对路径与固定DLL加载机制；不启动脚本解释器、不拼接shell命令、不向环境PATH寻找程序。
- 任务描述与IPC按版本化白名单验证；路径属于显式用户输入，仅存在于本地任务通道，不写入用户预设或可复制诊断的明文路径。
- 优先使用本用户访问范围内的命名管道和随机job ID；父进程负责创建、认证/关联自己启动的子进程、限制报文长度和频率。不要对外开放网络端口。
- progress必须来自真实编码计数/时长，包含source/generated/Hold/output，不能按墙钟计时伪造百分比。100%只在flush/mux/回读检查和最终重命名全部成功后出现。
- 取消通过本作业IPC通知循环、结束编码、释放资源、保留partial；强制终止仅作为有时限的故障兜底，限制本作业进程树，不能按进程名杀所有Veyra。
- 父进程退出时先给合作取消和收尾时限，防止孤儿GPU作业。若仍在导出，关闭窗口提示“取消导出并退出 / 返回”；这是用户可能损失长任务的明确选择，不是每次模式切换弹确认。
- 已存在输出/partial继续拒绝覆盖；不自动删除失败文件。

### 7.3 并存不等于免费GPU性能

- 允许返回日常观看，但两进程仍共享物理GPU；不能承诺重负载导出与4K4X播放同时满速。
- 提供“观看优先”（默认）与“导出优先”。它们只影响导出作业节奏，不改播放画质、不丢播放源帧。
- 观看优先可在有界源帧边界暂停/节流导出：结合播放实际提交迟到、视频负载与GPU资源错误；不通过每个pass CPU等待节流，不持锁等待。
- 首版需要证明前台会话不中断、导出能继续/暂停/取消、状态一致；若该硬件无法并存，明确标“等待GPU资源”并保留作业及前台播放，不无声停止播放。
- 如果前台因当前SR4K+4X本来就低于源率，UI应显示已有瓶颈；不可把所有落后归给导出。
- 日常小进度条点击“查看”切到Professional/Export，操作本身不启动/暂停/取消任务。

## 8. 原生UI实现路线与文件职责

### 8.1 技术路线

保留原生Win32窗口与D3D12视频宿主。用自绘深色控件和统一绘制/字体层实现参考图质感；建议用Windows SDK自带Direct2D/DirectWrite绘制UI面板、文字和功能图标，保留标准EDIT/COMBOBOX等输入语义并统一主题。若局部使用GDI，必须有一致的双缓冲、DPI及焦点表现。

本计划不把产品迁成网页/Electron/WebView，不引入新的前后端服务，不安装设计插件。HTML效果图如果后来单独制作，只能是设计预览，不能代替真实veyra.exe交付。

保留原生窗口操作、标题栏拖动/双击最大化、最小化/最大化/关闭、任务栏和键盘可访问性。自绘标题区不能只画三个无法操作的按钮；最终须验证实际Windows窗口行为。

### 8.2 建议文件树（拟新增，当前不存在）

```text
apps/veyra/
  main.cpp                       入口、消息分发、CLI兼容；不承载所有页面
  ui/AppShell.h/.cpp             稳定窗口壳、共享video宿主、模式控制
  ui/UiSessionState.h            纯界面状态
  ui/Layout.h/.cpp               DIP矩形计算、响应式布局，支持纯数据测试
  ui/Theme.h/.cpp                颜色、字体、绘制资源和统一控件状态
  ui/CommandRouter.h/.cpp        UI动作到服务；处理focus/快捷键边界
  ui/TransportBar.h/.cpp         文件播放/采集底栏、音量、字幕
  ui/DailyView.h/.cpp            日常入口、空状态、轻量菜单
  ui/ProfessionalView.h/.cpp     专业壳、侧栏、下方面板
  ui/EnhancementPanel.h/.cpp     模型/变化量/资源策略
  ui/FrameGenerationPanel.h/.cpp 倍率、光流、内容节奏
  ui/PresetPanel.h/.cpp          预设CRUD和应用
  ui/CapturePanel.h/.cpp         异步枚举、格式、音频、连接状态
  ui/ExportPanel.h/.cpp          配置/冻结信息/真实作业进度
  ui/DiagnosticsPanel.h/.cpp     性能详情、脱敏预览和复制
  ui/UiPreferenceStore.h/.cpp    UI偏好与成功启动设置
include/veyra/engine/ExportJobManager.h
src/engine/ExportJobManager.cpp
scripts/acceptance/ui-dual-mode.ps1
```

这是职责边界，不要求为凑文件机械拆每个小函数；但禁止继续把设置、作业、绘制、采集枚举全堆进main.cpp。

### 8.3 已有文件的改造规则

- SettingsWindow、TelemetryWindow分阶段提取可复用逻辑，再换嵌入式视图；删除旧入口前必须确保每项原功能在新界面可达。
- PresetStore改由应用服务持有一个实例，两种模式订阅同一状态；其磁盘schema兼容与损坏保护不变。
- PlayerSnapshot新增明确TransportState（Idle/Opening/Playing/Paused/Ended/Stopping/Failed）、source session ID和音量/字幕必要状态；running不再被UI解释成播放/暂停全部语义。
- 输入设备枚举/格式查询在专用COM线程或已明确所有权的worker完成，以消息回主线程；结果过期可丢弃，不把查询绑定到每次UI刷新。
- snapshot消费建议4Hz，只刷新变化文本；进度显示可以本地平滑，但不能用插值计数当实测帧率。
- Resize调用合并到最后目标尺寸；UI线程不drainQueue。视频呈现和字幕叠加的线程/fence所有权明确，布局代码不能跨线程释放纹理。
- Direct2D UI渲染与D3D12视频分层明确，不把视频纹理读到CPU供UI绘制。控件/字幕覆盖视频需解决真实HWND层级，不靠普通STATIC透明背景假装透明覆盖。

## 9. 边界场景与错误呈现

| 场景 | 必须表现 |
|---|---|
| 无媒体启动 | 日常空状态，打开视频/连接采集可达 |
| 媒体加载中切模式 | 加载任务继续；两个模式展示同一Opening状态 |
| 暂停中切模式/resize | 保持相同源帧与暂停状态，不能自动播放 |
| 播放结束 | 显示重播，只有用户点击才重新打开/seek |
| 无采集设备 | 明确未找到，刷新可用，不自动反复创建采集图 |
| 采集配置层关闭 | 保留此前活动源；无新增打开/关闭事件 |
| 实卡拔线 | 冻结/终止旧画面时明确“信号中断”，不要继续涨输入帧数 |
| 设置失败 | 保留旧Applied，显示回滚；当前预设标签对应真实生效值 |
| 缺失预设/配置损坏 | 原文件保留，诊断与默认策略明确 |
| 后台导出失败 | 前台继续，日常显示小型失败提示；专业展示原因和partial路径 |
| 模式快速反复点击 | 幂等、最后一次目标生效，不排队创建多窗口 |
| 全屏失焦/Alt+Tab | 释放临时原图按住，光标和焦点恢复；不重置NR历史 |
| 小窗口长文件名/超长设备名 | 省略+tooltip，关键操作不被挤出 |
| 诊断复制 | 仅复制已预览脱敏内容，后续新日志不能悄悄替换待复制内容 |

## 10. 施工顺序与每步退出条件

每个UI阶段只由主Agent写入；代码变更先留工作记录。独立Reviewer只读，不替Maker改代码。未经实际检查不能把清单预勾为完成。

### UI0：盘点、保护与新基线

工作：完整读AGENTS及现有修复报告；记录git status/diff/stat、EXE/runtime身份、受保护文件hash；保存现有dirty patch；列出所有旧控件到新位置映射。

退出：基线可恢复，删掉的测试媒体保持删除，现有文件不被覆盖；本次仅文档阶段的状态明确。禁止用rehash消除漂移。

### UI1：共享状态与动作合同

工作：UiMode/UiSessionState、TransportState、CommandRouter、Layout纯函数；一个video HWND贯穿两种模式；建立模式切换计数/日志。

退出：在现有窗口上先验证切模式不调用open/stop/requestSettings，无媒体/播放/暂停状态一致；纯布局测试覆盖DPI和窄窗口。此阶段不先堆漂亮卡片。

### UI2：统一深色控件与日常模式

工作：Theme、底栏、空状态、顶部源入口与专业切换；文件播放/暂停/进度/最近文件可用；窗口布局和键盘焦点完整。

退出：真实视频可看，普通窗口不再出现专业参数/导出按钮；日常与现有引擎行为一致。不能用静态视频截图当播放。

### UI3：专业工作台与参数/预设迁移

工作：专业导航、右侧四页、原图对比/分屏；提取SettingsWindow/PresetStore逻辑；Desired/Applied和错误回滚可见；明确总增强开关状态与可恢复设置。

退出：全部7+5参数及倍率/尺寸/光流可达，预设CRUD完整，返回日常保留成功设置；失败注入整套回滚且NVOF不丢；缓存重算不增源帧计数。

### UI4：音量/字幕与采集界面

工作：补音频命令和快照、字幕显示控制；采集枚举异步化、连接状态、两种模式共享会话；采集底栏不显示seek。

退出：合成音频证明音量/静音作用于本应用输出且不改音频时钟；无设备/配置失败状态通过；实卡项标awaiting_user_capture_test，不借模拟通过冒充设备通过。

### UI5：后台导出任务

工作：独立worker、冻结配置通道、进度/取消/暂停、父子进程清理、专业Export页与日常小进度。

退出：同一视频导出时前台继续播放，切模式不换job ID；前台改设置不改变导出冻结快照；H.264/HEVC/CFR/音轨/partial行为不退化；不能用现有startExport停止播放器后仍宣称并存完成。

### UI6：性能、诊断与负载提示

工作：迁移TelemetryWindow；尺寸链摘要、GPU/CPU分项、实际提交频率、错误摘要、诊断脱敏预览；导出竞争和重负载提示。

退出：无来源的数值显示未测；子进程导出与前台播放指标分开；错误真实码和上下文可查，复制前后内容一致。

### UI7：全屏、DPI、小窗口与输入收尾

工作：共享全屏逻辑、控制栏显隐、多个DPI、显示器变化、焦点/快捷键、长文本、分屏坐标。

退出：F11/Alt+Enter/双击/Esc、编辑框快捷键隔离、20次切换资源有界；只有必要的显示资源resize，无Feature重新创建。未具备真实多屏条件时如实标手工待验，不假装自动模拟等于物理通过。

### UI8：独立验收与反回归

工作：新增ui-dual-mode入口；跑下节关键用例；再运行原repair-v2相关回归；截取真实新EXE日常/专业截图和必要的状态截图。

退出：每条自动检查有本次run ID、真实退出码、日志与EXE哈希；每次≤300秒；无新的设备移除、无正常路径像素读回、无旧功能消失。视觉审核与功能审核分别记录。

### UI9：只读审查和交付

工作：新上下文Reviewer只读审当前差异/截图/新日志/遗漏；修发现后有针对性重测。更新交付状态、使用说明、WORKLOG与UI进度字段。

退出：输出新EXE与SHA256、文件清单、实际命令/耗时、日常/专业截图、模式切换身份/性能证据、后台导出证据、未验实卡项及唯一下一动作。旧Phase字段不被改造成新UI通过证明。

## 11. 验收矩阵与证据

### 11.1 核心自动检查

| ID | 检查 | 通过条件 / 证据 |
|---|---|---|
| M01 | 冷启动与重启 | 均Daily；有效增强设置按启动规则恢复；无自动连接采集 |
| M02 | 播放中往返模式20次 | 同一sourceSession/video HWND；mode-change不产生源重开/seek/Feature create/reset |
| M03 | 暂停中切模式 | PTS/源ID不变、pause状态不变；显示重绘不计新源帧 |
| M04 | 模式和设置同时变化 | 仅真实设置动作增加revision/必要epoch，布局动作不混入 |
| M05 | 模式切换间隔 | UI命令P95≤16ms、单次≤50ms；稳定60源fps额外提交间隔≤100ms，完整样本保留 |
| M06 | 参数编辑与预设 | 7+5值域、整数/NaN、整套应用/回滚、加载/保存、坏配置保留 |
| M07 | 总增强开关 | 关闭有真实作用、开恢复配置；不能只用comparison冒充关闭处理 |
| M08 | 源帧计数 | 缓存重算同sourceId，read计数不增；UI刷新/模式切换不增计数 |
| M09 | 导出并存 | 前台sourceSession/时钟持续，子job独立；输入/设置冻结，与前台后续变更隔离 |
| M10 | 导出生命周期 | 准备/运行/暂停/取消/失败/成功显示真实；partial保护，父进程退出无孤儿任务 |
| M11 | 导出内容 | 2X/3X/4X有效generated/Hold计数、96包240fps等合成合同保持；音轨检查 |
| M12 | 音量 | 无音轨显示能力状态；音量0/中间值/静音有真实增益证据，不改变其他应用/系统音量 |
| M13 | 键盘 | 输入框能输入V/空格；hidden控件不响应；全屏和focus恢复正确 |
| M14 | 尺寸布局 | 100/125/150/200%纯布局无负尺寸/越界/关键按钮重叠；图像contain |
| M15 | 诊断 | 实际错误码、源/作业区分、同fingerprint计数、64上限、脱敏预览与复制一致 |
| M16 | 既有产品回归 | SR4K分辨率、真实FG、参数回滚、NVENC、CFR量化/VFR拒绝保持有效 |

性能阈值是未来验收目标，当前未运行，不得预填PASS。硬件负载不达标应报告实际数据和影响，不修改分母或删除失败样本。

### 11.2 必须看真实窗口的视觉检查

- 日常播放、日常采集配置/无设备、专业增强、专业导出、展开诊断、全屏、小窗口至少7种状态。
- 真实文字清晰，重要按钮无裁切，中文名长文本不挤压；橙色强调有主次，视频比例正确。
- 原图/增强两侧在同一视频内容矩形，拖杆位置正确；暂态按住失焦后恢复。
- 不把源视频截图或设计稿合成图当作新EXE运行截图。
- 仅使用项目非私人合成媒体；不默认截取用户采集内容或桌面。
- 手工不可得的多屏/DPI穿越和采集断线必须标未测，不以代码有分支就写通过。

### 11.3 新验收入口（拟新增，本文编写时不存在）

```powershell
# 构建独立计时
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/build.ps1 -Root . -Preset x64-release

# 未来新增；一次调用包含的所有子测试共同遵守本次300秒
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/acceptance/ui-dual-mode.ps1 -Root . -MaxRuntimeSeconds 300 -Case all

# 已有repair-v2反回归，另一次调用也最多300秒
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/acceptance/repair-v2.ps1 -Root . -MaxRuntimeSeconds 300 -Case all
```

新入口支持state/layout/switch/settings/audio/export/visual/all等有限case；准备素材、子进程和probe都计入本次调用时限，超时记录并只停止自身进程树。不能通过拆无限子进程逃避单次时限，也不把历史累计耗时当未来剩余额度。

证据目录建议：`logs/ui-dual-mode/<run-id>/`，包含result.json、真实命令、stdout/stderr、EXE/hash、可验证的截图路径、每项status/exit/time、未执行原因。

建议日志事件：

```text
ui-mode: actionId, from, to, layoutRevision, sourceSessionId, videoHwnd
ui-mode-state: ptsBefore/After, transportBefore/After, settingsRevisionBefore/After
ui-mode-resources: featureCreate/reset/sourceOpen counters before/after
ui-layout: dpi, clientExtent, contentRect, mode, resizeRequested/Completed
export-job: jobId, workerPid, frozenRevision, status, source/generated/hold/output
```

GPU、Present、模式动作时间分开；要归因于mode-change，关联actionId而不是只比较总计数（实际scene cut/用户改设置可能合法改变epoch）。日志正常持续播放不可无界增长内存队列。

## 12. 对抗式审查清单

实现者交付前逐项回答，Reviewer至少抽查这些失败方式：

1. 所谓两种模式是不是实际关闭/重开了播放器？隐藏窗口里的worker是否仍然重复工作？
2. 专业切日常是否通过加载“日常预设”偷偷改了分辨率/倍率？
3. 分屏仍开但日常看不到关闭入口，是否让有效生成帧被隐性抑制？
4. 导出是否仍走旧startExport并停止前台？独立进程是否传齐全部设置，还是仅NR/SR/FG几个CLI开关？
5. 进度100%时是否真的完成mux、音轨和最终文件验证？取消是否只杀了UI而留下GPU进程？
6. 音量滑杆是否只是改了标签？采集音频路径与文件WASAPI是否分别有实际能力判断？
7. 快捷键是否抢走输入框的V、空格和方向键？隐藏控件是否还会提交设置？
8. 右栏收起/展开是否连续ResizeBuffers导致卡顿？暂停图片是否因为模式切换变黑？
9. UI字段、PresetStore和EngineController是否形成三份不同的设置真源？Applied失败时视觉是否仍显示成功？
10. 常驻性能更新是否同步文件I/O或GPU等待，导致UI新瓶颈？
11. “关闭增强”是否仍运行全负载，或者恢复时丢掉用户的模型/光流设置？
12. 在125/150%字体缩放与1366×768屏幕上是否有无法到达的导出取消/模式退出按钮？
13. 仅按钮改了中文，但错误、tooltip、焦点名称和导出状态是否还是原英文？
14. 既有SR4K+4X性能限制是否被改成漂亮的“240fps”理论数字？
15. 新文档中的“已实现/通过”是否有当前EXE和run ID，不是复制旧Phase或设计稿？

## 13. 最终交付与停点

最终交付应包含：

- 真实可运行新EXE及SHA256，仍由Veyra.cmd启动。
- 日常与专业实际窗口截图、操作说明、两种模式功能映射。
- 模式无重开/无参数改变的实测日志、DPI/全屏/键盘结果。
- 后台导出并存/冻结设置/取消结果，音量真实作用证据。
- 构建命令/退出码/独立耗时；每次短测耗时、run ID和失败/修复记录；不省略未测项。
- 只读独立Reviewer结论与范围；已知性能和硬件限制。
- 不改旧Phase字段、不制造公开首发完成状态、不打包专有DLL。

本文件的设计方向已得到用户确认。本次到文档交付为止，后续唯一施工起点是**UI0盘点保护 → UI1共享状态与稳定视频宿主**，不是直接在main.cpp上堆另一排按钮。
