# RTX 4060 持续欠速与音频卡顿诊断

后续执行方案：[连续音频与实时视频调度修复](REALTIME_AV_SCHEDULING_REPAIR_PLAN_2026-09-11.md)。当前仍为方案阶段，未修改0.0.4产品行为。

用户在0.0.4中发现：60fps文件开启2X补帧，未达到120输出时仍有音频卡顿。此次只核对用户日志与当前源码，没有修改产品、测试或已发布资产。

## 日志事实

输入日志 `C:/Users/123/Desktop/veyra-app.log`，本机只读分析后副本保存在忽略目录 `logs/4060-audio-20260911/veyra-app.log`，不提交原始日志。设备为RTX4060、7957MiB、驱动32.0.15.9186。主要会话为1080×1920、60fps文件，AAC44100Hz到WASAPI48000Hz；不是采集卡会话。

按相邻 `player-timing` 的真实时间戳计算 `processed` 和 `displaySubmits` 累计计数差，只取同revision、0.5–2秒间隔及非负计数差；结果为这些窗口的速率均值，不是理论倍率FPS，也不是屏幕扫描帧率。重建/暂停窗口不作连续播放证明。

| 设置revision | 样本窗口 | 源帧推进均值 | 呈现提交均值 | 说明 |
| --- | --- | --- | --- | --- |
| 8 | 29 | 47.39fps | 47.39fps | 未启用FG也持续低于60 |
| 11 | 32 | 51.45fps | 102.89fps | DLSS2X/NVIDIA光流，约07:32:54–07:33:26 UTC |
| 12 | 3 | 49.87fps | 99.73fps | DLSS2X/AMD光流，窗口短 |
| 13 | 15 | 52.96fps | 52.96fps | XeSS子帧由SDK呈现，这列不可直接当总输出FPS |
| 15 | 40 | 59.67fps | 59.67fps | XeSS另一设置，接近源时间线速度 |

revision11退出时原帧呈现1712、生成帧呈现1711、生成帧过期丢弃0，符合逐对处理/逐帧呈现，未见通过舍弃生成工作主动追赶。正常日志没有记录每次AudioVideoContinuity状态变化；不能从该日志直接给出声音暂停次数或精确听觉时间点。未发现已记录的音频端点恢复事件，但这不足以排除所有音频问题。

## 代码归因与限制

`AudioVideoContinuity.h` 仍在持续领先超20ms达100ms或领先80ms时进入等待，并在视频覆盖追上后恢复。`WasapiAudioSink.cpp` 将等待应用到WASAPI暂停。0.0.4修复短暂处理抖动与XeSS覆盖长度，没有改变持续欠速时靠停放音频维持时间线的策略。结合用户听感与日志欠速，周期性重缓冲是主要解释；日志缺少逐次音频事件，不能声称已做4060声学复现。

必须区分“输出帧数不足”和“源媒体时间推进不足”：60原帧+50生成帧仍可一倍速输出110张；55原帧+55生成帧只推进55/60秒媒体时间。后者每墙钟秒落后约83.3ms，固定增加缓冲或提高暂停阈值只能推迟再次不同步。

## 后续修复方向

1. 为欠速/音频等待新增带PTS、覆盖范围、领先量及原因的有界状态转换日志，不能只记FPS或成功计数。
2. 实时预览以音频连续和源媒体时间推进为优先级；补帧预算不足时，在生成工作提交前减少可选生成工作，跳过过期生成呈现。只在生成完成后丢帧不能收回其GPU成本。DLSS与XeSS的控制边界分别核对。
3. 单独NR也低于源帧率时，仅少补帧不足以解决。必须有明确的处理降档/旁路或视频预览跳帧策略；现有“不丢文件源帧”规则与自动降画质决定需明确处理，不能偷偷改为丢源帧，不能波及离线导出的完整性要求。
4. 后续回归应覆盖60→120仅够110的持续欠速、NR单独欠速、间歇抖动、恢复足额预算、暂停/seek/设置重建。验收检查音频连续性与媒体时间速率，不能将“偏差有界但周期性停音频”当作连续性通过。

本轮未运行新的构建、RTX runtime或4060硬件测试；仅分析用户4060运行日志及本地0.0.4源码。没有新的发布。
